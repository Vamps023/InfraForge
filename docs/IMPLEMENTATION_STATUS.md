# InfraForge implementation status

This file is the authoritative high-level implementation ledger. Design documents describe required behavior; they do not prove implementation.

## Status vocabulary

- **Implemented**: production code exists and is reachable through its real entry point.
- **Integrated**: implemented across all required process/domain boundaries.
- **Verified**: the relevant automated/manual verification has been executed and recorded.
- **Not implemented**: design is approved but production code does not exist yet.

## Current state

| Area | Status | Evidence |
|---|---|---|
| Repository initialization | Implemented | `README.md`, repository conventions, source-of-truth docs |
| Product requirements | Implemented as specification | `docs/00_PRODUCT/PRD.md` |
| Technical architecture | Implemented as specification | `docs/01_ARCHITECTURE/TRD.md` |
| Accepted architecture decisions | Implemented as specification | `docs/ADR/` |
| Native C++ executable baseline | Verified | `engine/src/main.cpp`; GitHub Actions native build/self-check succeeded on foundation commit `2efa77aa6a8300b8d798573f64b7eeacbd4873c3` |
| Native dependency manifest | Verified | `vcpkg.json` (ixwebsocket, protobuf, sqlite3, nlohmann-json, doctest, proj) pinned to immutable registry baseline; full native build passed on Windows and Linux in the native workflow for commit `28d46b877c8cc30759e9f653876c3afa226c8056`; PROJ 9.8.1 added for the georeference runtime (verified in the local Release build) |
| Protocol Buffer foundation schema | Verified | `contracts/proto/infraforge/protocol/v1/foundation.proto` + `project.proto`; Buf lint and TypeScript generation pass in CI |
| TypeScript protocol generation | Verified | Buf generation + strict TypeScript + production frontend build passed in CI (web/desktop workflow) |
| Authenticated loopback WebSocket server | Verified | `engine/src/network/WebSocketServer.cpp`; token/protocol handshake exercised end-to-end by `tools/engine-smoke/smoke.ts` against the real engine process on Windows and Linux; connections register for event delivery only after successful authentication (`engine/tests/WebSocketServerTests.cpp`) |
| Project lifecycle (create/open/save/save-as/close/summary) | Verified | `engine/src/application/ProjectService.cpp`, `engine/src/persistence/SqliteProjectStore.cpp`; SQLite create → close → reopen with identical canonical metadata verified end-to-end by `npm run verify:engine` (real engine, authenticated WebSocket, no mocks) and by `infraforge-engine --self-check`; doctest suite `infraforge-engine-tests` covers store/service/processor semantics including newer-schema rejection without modification, journal-mode preservation on rejected opens, migration-then-reopen continuity, save atomicity (manifest untouched), and corrupt-database reporting as `COMMAND_ERROR_CODE_PERSISTENCE_FAILURE`; event delivery is restricted to authenticated connections with an in-process two-client isolation test (`engine/tests/WebSocketServerTests.cpp`) |
| Project format (`.iforge` + `project.json` + `project.db`) | Verified | `engine/src/persistence/ProjectManifest.cpp`, `engine/src/persistence/SchemaMigrations.cpp`; strict manifest validation, forward-only migrations, schema v1 (`project_state`, `georeference`) + migration 2 (`origin_height`) covered by persistence tests including v1→v2 upgrade |
| Project domain documentation | Implemented as specification | `docs/05_DOMAINS/PROJECT.md` |
| Electron desktop shell | Build verified | secure `main.ts`/`preload.ts`; desktop TypeScript/build passed in CI |
| Desktop engine supervision | Build verified; runtime integration pending native runtime | `apps/desktop/src/EngineSupervisor.ts`; random token, bounded bind retries, readiness validation |
| Desktop OS directory dialogs | Build verified | `apps/desktop/src/main.ts` `dialog:pick-directory`, narrow preload API `apps/desktop/src/preload.ts` |
| React editor shell | Build verified | `apps/frontend/src/App.tsx`, `styles.css`, UI-only Zustand stores; strict TypeScript and production build passed in CI |
| Editor shell framework (command registry, docking, outliner, inspector, operations, problems) | Build verified; runtime pending desktop runtime | GitHub issue #5. `apps/frontend/src/editor/` — one typed command registry (`commands/commandRegistry.ts`) drives the in-app menu (`shell/AppMenu.tsx`), toolbar (`shell/Toolbar.tsx`), keyboard shortcuts (`commands/useCommands.ts`), and command palette (`commands/CommandPalette.tsx`); existing real project operations migrated to `commands/builtinCommands.ts` (no duplicated execution logic). Shared `isCommandVisible`/`commandSurfaces` helpers keep visibility and surface filtering consistent across all surfaces. Dockable/resizable layout (`layout/`) with left/right/bottom panel regions, pointer + keyboard resize handles, minimum/maximum sizes, and USER preference persistence under `localStorage` key `infraforge.editor.layout.v1` (separate from canonical project state, ADR-0009). Virtualized outliner (`outliner/`) consumes registered backend/domain projections (`outlinerProjection.ts`) keyed by stable canonical IDs; `outlinerTree.ts` flattens with expansion state, `Outliner.tsx` window-renders visible rows; `projectRootProjection.ts` surfaces the real canonical project root from the backend-projected `ProjectSummary` (no fake children); empty-state distinguishes no project / no entities / no search match. Inspector section registry (`inspector/inspectorRegistry.ts`) resolves sections by canonical selection; `projectOverviewSection.tsx` displays the real canonical project summary and applies when no entity is selected OR when the project root (canonical UUID) is selected. Operations store (`operations/operationsStore.ts`) is backend-projected, starts empty, no fabricated jobs; cancellation modeled but disabled until the protocol supports it; backend event ingestion is implemented — the engine emits job.queued/job.started/job.progress/job.completed/job.failed events from the canonical CommandProcessor path and shellEventProjector.ts projects them into the store with bigint preservation and terminal-state protection (protocol 1.3). Problems/diagnostics store (`problems/problemsStore.ts`) starts empty, no sample warnings; backend diagnostic event ingestion is implemented — the engine emits diagnostic.added/diagnostic.removed/diagnostic.cleared events for the vertical-reference georeference condition and shellEventProjector.ts projects them into the store with shell-owned diagnostic preservation (protocol 1.3); `useProblemDiagnostics.ts` bridges real engine/viewport errors; backend diagnostic event ingestion awaits the future `diagnostic.added`/`diagnostic.removed`/`diagnostic.cleared` protocol families and engine implementation. Centralized availability predicates (`availability.ts`) gate commands by engine/project/viewport state. Selection (`selection/selectionStore.ts`) holds canonical IDs only, never frontend object identity. Accessibility: menu Arrow/Escape, tablist/tab ArrowLeft/Right, tree ArrowUp/Down/Left/Right/Enter, Problems rows focusable + keyboard-activatable, ResizeHandle aria-valuemin/valuemax + unmount-during-drag cleanup. Architecture documented in `docs/01_ARCHITECTURE/EDITOR_SHELL.md`. Covered by 243 vitest tests across command registry, command palette, command surfaces, builtin commands, availability, layout persistence/restoration/resize, outliner tree/virtualization/selection/keyboard, inspector registry, project overview section, operations, problems, and bottom panel accessibility (run in the web/desktop workflow). No fake domain content; the native viewport visibility policy path is unchanged |
| Frontend authenticated engine session | Build verified; end-to-end runtime pending desktop runtime | `apps/frontend/src/lib/engineSession.ts`; command correlation + event subscription over the generated protocol; the frontend declares its own generated protocol version rather than echoing the engine's advertised one |
| Frontend project lifecycle actions | Build verified; end-to-end runtime pending desktop runtime | `apps/frontend/src/features/project/` — New Project dialog, Open/Save/Close actions wired to real engine commands; projections from backend results/events only. The New Project horizontal-CRS field is a searchable, keyboard-navigable picker over a curated common-CRS preset list (`commonCrsPresets.ts`, UI metadata only — the engine Geo service stays the sole CRS validation/canonicalization authority); selection writes exactly the CRS identifier into the existing `horizontalCrs` value, manual/free-text entry is preserved, geographic presets are visible, searchable, and marked invalid-as-project-CRS without substitution, and the engine's geographic-CRS rejection gains a plain-language explanation with the engine detail kept. Covered by 22 vitest frontend component/preset tests (run in the web/desktop workflow) |
| Engine protocol smoke verification | Verified | `tools/engine-smoke/smoke.ts`; `npm run verify:engine` launches the real engine and exercises hello → create → summary → save → close → reopen with event-stream checks; passed on Windows (repeated local runs) and Linux in the native workflow for commit `28d46b877c8cc30759e9f653876c3afa226c8056` |
| Web/desktop CI | Verified | protocol lint, strict TypeScript, frontend build, and desktop build succeeded on GitHub Actions for commit `28d46b877c8cc30759e9f653876c3afa226c8056` |
| Graceful project-aware engine shutdown | Not implemented | No project transaction lifecycle exists yet; current supervisor terminates child on app quit; engine flushes SQLite session at exit. Tracked in `docs/05_DOMAINS/PROJECT.md` limitations |
| Canonical georeference runtime | Geo runtime verified; renderer boundary API implemented; renderer consumption pending integration | GitHub issue #3. `engine/src/domain/geo/` (PROJ-backed `GeoTransformService`, canonical `GeoreferenceConfig`, `RenderLocalFrame` boundary API), `application::GeoService`, `geo.*` commands + `georeference_changed` event (proto 1.2), schema migration 2 (`origin_height`), `ProjectStore::updateGeoreference`, frontend `features/geo/` projection + settings panel. Verified by `GeoTransformServiceTests`/`GeoPersistenceTests`/`GeoServiceTests`/`CommandProcessorTests`, `infraforge-engine --self-check` control point, and `npm run verify:engine` (real engine: query → transform control point → set → save → reopen persistence, event stream). Renderer consumption lands with the viewport domain (issue #2) |
| Native viewport hosting (child surface) | Implemented; runtime verified on Windows | `viewport/` module (`infraforge-viewport` process): real child HWND parented into the shell window, stdio control protocol (place/visibility/shutdown), placement from page rect + window content bounds converted with `screen.dipToScreenRect` (per-display DIP→physical on mixed-DPI setups), Chromium re-parent self-healing, per-monitor-v2 DPI awareness enabled before window creation (doctest-guarded), class brush lifecycle registered/unregistered with the child window class. Supervisor startup-failure semantics (readiness timeout kills the spawned child, no orphaned process, restartable after failure) are covered by `apps/desktop/tests/ViewportSupervisor.test.ts` (real child processes via vitest, run in the web/desktop workflow). Blocking application overlays (New Project dialog, Georeference panel) hide the native viewport through a centralized shell visibility policy (`apps/desktop/src/ViewportVisibilityPolicy.ts`, vitest-covered): the page reports host-visible/no-overlay over the existing `viewport:set-visible` channel, the shell folds in window displayability, and restoration recomputes placement from the latest cached bounds before showing (no stale coordinates, no flash over the modal). The native surface is always created hidden — no startup timing (overlay/minimize state changing while the process launches) can produce a visible window, because the first show happens only through the post-readiness policy application (place -> show) or later control commands. Verified live with repeated New Project open/close cycles, project creation through the modal, the Georeference panel, minimize/restore, maximize/restore, and clean shutdown with no orphan viewport process; a killed viewport process keeps reporting stopped and is never resurrected to ready by a visibility change. A live 100% ↔ 125/150% mixed-DPI monitor change under the Electron shell remains an outstanding manual acceptance step |
| Vulkan renderer core + grid pass | Implemented; verified | `viewport/src/renderer/`: Vulkan 1.3 instance/device/surface/swapchain with RAII ownership, swapchain lifecycle state machine with explicit acquire-result handling (out-of-date recreates before retry, suboptimal acquire renders and presents the acquired image before recreating so the acquire semaphore is consumed, timeout/not-ready skip the frame, surface-lost/memory/device-lost fail the renderer explicitly, zero extent suspends), render thread with guaranteed join-after-self-stop (`RenderThread`), embedded-GLSL grid pass (shaderc) with RAII staging upload, orthographic grid camera, selection-ID registry contract. Verified live on NVIDIA GeForce RTX 3060: world grid renders in the editor viewport; maximize/restore/monitor-change/clean-shutdown run with KHONOS validation layers enabled and zero validation findings. 32 doctest cases cover the acquire/present decision table, surface format/colorspace pair selection, render-thread join discipline, selection ids, and camera math (`infraforge-viewport-tests`); native (Windows and Ubuntu, build + doctest suite + engine self-check + `verify:engine`) and web/desktop workflows green on the rebased branch for commit ed94d2ba9faef51a3767070c2e9abf191ce22eb7 |
| Renderer status projection | Implemented; runtime verified on Windows | Starting/ready/suspended/recreating/device_lost/failed/stated records flow viewport → shell → React status bar with GPU name and Vulkan version (`Renderer ready · NVIDIA GeForce RTX 3060` observed live); visibility control commands re-assert the renderer's last explicit record instead of ever implying readiness for a dead renderer (doctest-covered policy, `visibilityStatusReport`); validation mode via `INFRAFORGE_VIEWPORT_VALIDATE=1` (requires the KHONOS validation layer to be provisioned locally, e.g. from the Vulkan SDK or vcpkg's `vulkan-validationlayers`; it is deliberately not a build dependency) |
| World partition runtime (domain foundation) | Implemented (in-memory domain foundation, issue #4); renderer/terrain/road consumption pending integration | `engine/src/domain/world/` + `docs/05_DOMAINS/WORLD.md`: canonical double-precision `SpatialBounds` in project-global space (closed edges, empty state, negative-coordinate semantics), `ChunkGrid` with the physical 1 km default edge converted through the project's resolved canonical linear unit (`ChunkGrid::fromMetreEdge`; metre and US-survey-foot projects get physically identical cells; explicit `ChunkGridConfig` for project-unit edges; floor-division mapping, conservative closed-boundary enumeration, integer-exactness ceiling ±(2^53 − 1) plus per-grid cell-footprint representability validation (collapsed boundary products fail loudly — the usable extent depends on the configured chunk size, not one universal world limit)), sparse `SpatialIndex` whose mutations return the canonical chunk diff (`IndexMutation`: previous = old-bounds cells, updated = new-bounds cells, dirty = union) with explicitly declared typed `InvalidationClass`/`InvalidationMask` (geometry/material/topology/terrain/simulation), `ChunkDirtySet` per-cell accumulation, and per-cell content generations (`lastAffectingRevision` advancing only for dirtied cells, so distant local edits leave unrelated chunk caches current), `ChunkResidencyTracker` (unloaded/loading/resident/stale/evicting with validated transitions), and versioned `ChunkCacheMetadata` freshness (schema/generator/per-chunk-source revisions). Deliberately no protocol/persistence surface. Verified by doctest suites `SpatialBoundsTests`/`ChunkGridTests`/`SpatialIndexTests`/`ChunkResidencyTests`/`ChunkCacheMetadataTests`/`LargeWorldIndexTests` — the 100 km × 100 km acceptance case inserts 2,000 entities across all quadrants, cross-checks every occupied cell against independent ground truth (occupied ≪ the 10,000-cell dense matrix), matches brute-force queries at ±49 km including negative coordinates, and proves a one-entity edit dirties ≤ 4 chunks (~0.04% of the dense grid), advances only the edited cells' content generations, and stales only resident dirty cells; full engine suite 125 cases / 1258 assertions green with MSVC /W4 /WX plus `infraforge-engine --self-check` and the `verify:engine` protocol smoke; native (Windows and Ubuntu: build, doctest suite, self-check, `verify:engine` protocol smoke) and web/desktop workflows green for commit 9a4a3b43e54ca30c036aa781d8adeb6ab3804e43 |
| Terrain | Partially implemented; Issue #6 remains open | Local-file GeoTIFF/DEM ingestion, canonical sampling, persistence, derived terrain tiling/LOD, editor-shell projection, and Vulkan terrain code exist on PR #28 and have substantial automated coverage. Issue #6 `Download Area` is now implemented with a real production terrain DEM provider: **AWS Terrain Tiles (Terrarium)** — real HTTP fetch via ixwebsocket, Terrarium PNG decoding via GDAL, proper GeoTIFF output with EPSG:3857 CRS and NoData. The Download Area UX uses a real interactive Leaflet map with explicit Navigate/Draw Area modes (pan, zoom, draw rectangle, go-to lat/lon, location search via Nominatim, selection grid overlay, per-tile click selection, 1/2/4/8/16 km tile sizes). Production `TerrainService` registers only real providers via dependency injection (`MockTerrainProvider` is test-only and excluded from production registries). HTTP cancellation is genuinely interruptible: the `CancellationCallback` (returns `bool`) is polled by the ixwebsocket progress callback, so in-flight requests abort promptly when the JobSystem requests cancellation. Provider attribution is exposed through `terrain.list_sources`, persisted on canonical `TerrainDataset`, and shown in the Download Area UI and Terrain Inspector. Canonical raster ingestion uses GDAL to mosaic provider tiles into a single project-owned GeoTIFF with real CRS, geotransform, dimensions, and NoData — no MDEM/custom binary. Sparse/disconnected coverage is persisted as canonical coverage pieces (one per selected application tile) in a forward schema migration; unselected gaps return `OutsideCoverage` (not merely NoData), and derived tile/scene generation excludes gap-only chunks. Provider errors are typed (auth, rate limit, timeout, etc.) with bounded retry and preserved through `JobRecord` → protocol → frontend. End-to-end cancellation flows through provider/HTTP/assembly with interruptible backoff and cleanup; cancelled downloads end in `JobState::Cancelled` (not `Completed`) with no canonical dataset and no orphan temp files. Selection grid and provider request counts are bounded with checked arithmetic. Canonical raster assembly is strip-based with bounded memory and dimension validation. Download workers use snapshotted payload data (no executor-owned state access). Stale plan responses are ignored via generation guards. Tile size and provider changes reset selection and invalidate the plan. Download-start errors are surfaced to the user through the form error UX. See `docs/05_DOMAINS/TERRAIN.md` and `docs/03_PROTOCOL/COMMANDS.md`. PR #28 remains under merge-gate review; manual runtime verification pending. |
| Roads/lanes/junctions | Not implemented | GitHub issues #7 and #8; design only |
| Traffic infrastructure | Not implemented | GitHub issue #11; design only |
| Traffic simulation | Not implemented | GitHub issue #12; design only |
| Rail | Not implemented | GitHub issue #13; design only |
| World validation | Not implemented | GitHub issue #14; design only |
| Export/interoperability | Not implemented | GitHub issue #15; design only |
| Packaging/security/recovery | Not implemented | GitHub issue #16; design only |

## Verification rule

No entry may be promoted to **Implemented**, **Integrated**, or **Verified** without concrete source paths and verification evidence. The native protocol/project rows were promoted after the Windows and Linux native workflow (build, doctest suite, self-check, engine protocol smoke) and the web/desktop workflow passed for commit `28d46b877c8cc30759e9f653876c3afa226c8056`.
